[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ruff==0.11.13",
    "python-dotenv==1.1.0",
]

[tool.ruff]
line-length = 120
